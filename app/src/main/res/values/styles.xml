<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Stile personalizzato per la BottomAppBar -->
    <style name="CustomBottomAppBar" parent="Widget.MaterialComponents.BottomAppBar">
        <item name="android:paddingBottom">0dp</item>
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetEnd">0dp</item>
        <item name="fabCradleMargin">0dp</item>
        <item name="fabCradleRoundedCornerRadius">0dp</item>
        <item name="fabCradleVerticalOffset">0dp</item>
    </style>
    
    <!-- Stile personalizzato per la BottomNavigationView -->
    <style name="CustomBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="itemIconSize">24dp</item>
        <item name="labelVisibilityMode">labeled</item>
    </style>
</resources>
